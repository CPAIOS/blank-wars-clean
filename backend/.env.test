# Test Environment Configuration
NODE_ENV=test

# JWT Configuration
JWT_SECRET=test-jwt-secret-key-for-testing-only-very-long-secret-32-chars-plus
JWT_REFRESH_SECRET=test-jwt-refresh-secret-key-for-testing-only-very-long-secret-32-chars-plus

# Database Configuration (use in-memory for testing)
DATABASE_URL=:memory:

# Redis Configuration (will be mocked in tests)
REDIS_URL=redis://localhost:6379

# Server Configuration
PORT=3001
SERVER_ID=test-server-1

# Disable logging in tests
LOG_LEVEL=silent
